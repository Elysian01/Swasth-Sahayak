import { StyleSheet, Text, View } from "react-native";
import React from "react";

const PatientToken = () => {
	return (
		<View>
			<Text>PatientToken</Text>
		</View>
	);
};

export default PatientToken;

const styles = StyleSheet.create({});
